# 第9章: 本番デプロイ最終調整（WebSocketの生命線を本番で確定させる）

第6章で「ロジック層」を、第7〜8章で「地図とGeolocation」を本番に載せてきました。機能としてはこれで完成ですが、実は**まだ本当の意味では確定させていないこと**があります。それは「LiveViewのWebSocketが、リバースプロキシ(Traefik)越しに、本当に安定して繋がり続けているか」です。

これまでの章でも動いているように**見えては**いました。しかしそれは「TLS証明書がたまたま有効で、`check_origin`がたまたま一致していて、ブラウザがたまたま初回接続に成功していた」だけかもしれません。この章では、**見た目の動作確認では気づけない、本番特有の落とし穴**を1つずつ潰していきます。第0,6章に続く最後のデプロイチェックポイントであり、このチュートリアル全体の締めくくりでもありますにゃ。

---

## この章でじっくり学ぶこと

- **TLS終端(TLS Termination)という構造**: なぜDokploy(Traefik)がHTTPSを受け、コンテナの中では素のHTTPが流れているのか
- **`X-Forwarded-Proto`と`force_ssl`の罠**: プロキシ構成特有の「無限リダイレクトループ」がなぜ起きるのか
- **WebSocketが本当に繋がっているかの見分け方**: LiveViewには「繋がらなくても一見動いて見える」フォールバック機構がある
- **セッションCookieのセキュリティ属性**: `secure` / `same_site`がWebSocketハンドシェイクに与える影響
- **障害からの自己復帰**: コンテナ再起動時に何が消え、何が残るのか(第3章のGenServerキャッシュを例に)

---

## Step 1: Traefik(Dokploy)がやっていることを正しく理解する

DokployはコンテナのTLS終端を自動的に行い、内部的にはTraefikというリバースプロキシがブラウザとコンテナの間に立ちます。

```
ブラウザ ──(HTTPS, wss://)──▶ Traefik(Dokploy) ──(HTTP, ws:// 平文)──▶ Phoenixコンテナ
```

**ここが最初の盲点です。** コンテナの中で動くPhoenixから見ると、リクエストは常に**素のHTTP**で届きます。TLSはTraefikの層で終わっているためです。しかし第6章で設定した`config/runtime.exs`の`url: [scheme: "https"]`は、あくまで「PhoenixがURLを生成するときにどのスキームを使うか」という設定であって、**実際に届いたリクエストがHTTPだったかHTTPSだったかをPhoenixに教えるものではありません**。

Traefikは代わりに`X-Forwarded-Proto: https`というヘッダーを付けてコンテナに転送します。Phoenixがこのヘッダーを信頼するよう明示的に設定しないと、「ユーザーはHTTPSでアクセスしているのに、Phoenixは自分がHTTPで応答していると誤解する」というズレが生まれますにゃ。

---

## Step 2: `force_ssl`のリダイレクトループを防ぐ

Phoenixには「HTTPアクセスを自動的にHTTPSへリダイレクトする」`force_ssl`という機能があります。素朴に有効化すると、今回の構成では**無限リダイレクトループ**に陥ります。

```elixir
# 悪い例（このままでは無限リダイレクトになる）
config :weather_ex, WeatherExWeb.Endpoint,
  force_ssl: [hsts: true]
```

**何が起きるか**: ブラウザ→Traefik(HTTPS)→コンテナ(HTTP、内部的には常にHTTP)。Phoenixは「リクエストがHTTPで来た」と認識し、`force_ssl`が「じゃあHTTPSにリダイレクトしなきゃ」と302を返します。ブラウザはHTTPSで再アクセスしますが、Traefikを経由すると結局コンテナにはまたHTTPで届き、また302…これが無限に繰り返されます。

正しくは、`X-Forwarded-Proto`ヘッダーを信頼する設定を加えます。

```elixir
# config/runtime.exs
config :weather_ex, WeatherExWeb.Endpoint,
  url: [host: host, port: 443, scheme: "https"],
  check_origin: ["https://#{host}"],
  force_ssl: [rewrite_on: [:x_forwarded_proto]],
  http: [port: String.to_integer(System.get_env("PORT") || "4000")],
  secret_key_base: secret_key_base,
  server: true
```

`rewrite_on: [:x_forwarded_proto]`は、「`X-Forwarded-Proto`ヘッダーの値を、リクエストの実際のスキームとして書き換えて扱ってよい」という許可です。これにより、Phoenixは「あ、このリクエストはTraefikの向こう側ではHTTPSだったんだな」と正しく認識し、無意味なリダイレクトを発行しなくなりますにゃ。

**Nuxtとの対比**: Vercelのようなホスティングでは、この種のプロキシ透過設定はプラットフォーム側がほぼ完全に隠蔽してくれます。自前でTraefik/Dokployを運用する今回の構成では、この「プロキシとアプリの間の握手」を明示的に書く必要がある、というのが自前運用ならではの学びですにゃ。

---

## Step 3: WebSocketが「本当に」繋がっているかを見分ける

**ここが今回で一番重要な確認です。** LiveViewのJavaScriptクライアントは、WebSocket接続に失敗すると自動的に**ロングポーリング(long polling)**という代替手段にフォールバックします。これは「接続できないと真っ白な画面になる」のではなく、**遅いけれど一見動いているように見えてしまう**ため、正しい確認方法を知らないと本番障害に気づけません。

### 確認手順

1. ブラウザの開発者ツールを開き、Networkタブを表示する
2. フィルタを「WS」(WebSocket)に絞る
3. 本番URLの`/weather`を開く
4. `websocket`という名前の接続が張られ、ステータスが**`101 Switching Protocols`**になっていることを確認する

もし`WS`タブに何も表示されず、代わりに`longpoll`という名前で数百ミリ秒おきにリクエストが繰り返されているなら、**WebSocketへのアップグレードに失敗し、ロングポーリングにフォールバックしている状態**です。機能は動いて見えますが、サーバー負荷が高く、リアルタイム性も損なわれた状態ですにゃ。

### WebSocketが確立しない場合の主な原因切り分け

| 症状 | 主な原因 |
|---|---|
| WSリクエストが`403` | `check_origin`の値と実際のドメインが不一致(第6章参照) |
| WSリクエストが`400`や接続直後に切断 | TLS証明書の問題、またはTraefikがWebSocketアップグレードヘッダーを転送していない |
| WSは`101`だが数秒で切断を繰り返す | プロキシ側のアイドルタイムアウト設定がLiveViewのハートビート間隔より短い |
| ローカルでは繋がるが本番だけダメ | Step 2の`force_ssl`リダイレクトループ、または`PHX_HOST`の設定ミス |

Dokploy(Traefik)は基本的にWebSocketのアップグレードヘッダー(`Connection: Upgrade`, `Upgrade: websocket`)を自動で正しく転送してくれますが、カスタムドメインを使っている場合はTLS証明書がそのドメインに対して正しく発行されているか(Let's Encrypt発行が完了しているか)も合わせて確認してくださいにゃ。

---

## Step 4: セッションCookieのセキュリティ属性を確認する

LiveViewのWebSocket接続は、初回HTTPリクエスト時に発行されたセッションCookieを使って「誰の接続か」を検証します。このCookieの属性が本番のHTTPS環境と噛み合っていないと、WebSocket接続そのものが確立しない、または断続的に切れることがあります。

```elixir
# lib/weather_ex_web/endpoint.ex
@session_options [
  store: :cookie,
  key: "_weather_ex_key",
  signing_salt: "your-salt-here",
  same_site: "Lax"
]
```

> **注意**: `signing_salt`の値は`mix phx.new`実行時に自動生成されるランダムな文字列です。実際のプロジェクトでは「your-salt-here」ではなく、`config/runtime.exs`や環境変数から読み込まれた固有の値が使われています。ここを変更すると既存のセッションがすべて無効になるので、基本的に触らないことにゃ。

`force_ssl`を有効にした本番環境では、Phoenixは自動的にCookieへ`Secure`属性を付与します(HTTPS接続でのみ送信される)。Step 2の`rewrite_on`設定が正しく入っていないと、Phoenixが「今はHTTPだ」と誤解したまま`Secure`Cookieを発行してしまい、**ブラウザがそのCookieを送り返せず**、結果としてWebSocket接続が拒否される、という間接的な形でStep 2の設定漏れが響いてくることがありますにゃ。

---

## Step 5: 障害復帰の確認（コンテナ再起動で何が消えるか）

最後に、実運用を見据えて「コンテナが落ちて再起動した時に何が起きるか」を確認しておきます。Dokployのダッシュボードから手動で再起動してみましょう。

1. Dokployダッシュボード → 該当サービス → **「Deploy」または「Restart」ボタン**を押す
2. ブラウザ側で以下を確認:
   - [ ] 再起動中、ブラウザは自動的に再接続を試み続ける(LiveViewの標準機能。画面に`phx-loading`/切断中の見た目が一瞬出ることがあります)
   - [ ] 再起動完了後、自動的に接続が復帰し、画面が正常表示に戻る
   - [ ] 第3章で作った`StationCache`(GenServer)の中身は**再起動でリセットされる**ことを確認する(起動直後のログで観測地点マスタの再取得が走っているはず)

**深掘りポイント**: これは障害ではなく**設計通りの挙動**です。第3章で学んだ通り、GenServerの状態はプロセスと運命を共にするメモリ上のキャッシュであり、永続化されたデータベースではありません。コンテナが再起動すればプロセスも作り直され、キャッシュも空から再構築されます。これが許容できる設計だったからこそ、第3章では「観測地点マスタ」という**多少古くても実用上問題ないデータ**をキャッシュ対象に選んでいた、ということをこの章で改めて実感できますにゃ。

---

## つまずきやすい点

- **`force_ssl`を有効にした瞬間に本番だけ真っ白になる/リダイレクトが終わらない**: `rewrite_on: [:x_forwarded_proto]`の設定漏れがほぼ確実な原因。ブラウザの開発者ツールでNetworkタブの「redirect chain」を見ると、同じURLへの302が延々と繰り返されているのが確認できるにゃ
- **「動いているように見える」の罠**: ロングポーリングへのフォールバックは、機能テストだけでは気づけません。**必ずNetworkタブでWSの`101`を目視確認する**習慣をつけること。第6章のチェックリストで「時計が動いている」ことだけを確認していた場合、実はロングポーリングで動いていた可能性がある点に注意
- **カスタムドメイン切り替え時の`check_origin`更新漏れ**: Dokployが割り当てる一時ドメインから独自ドメインへ切り替えた際、`PHX_HOST`環境変数を更新し忘れると`check_origin`が古いドメインのままになり、新ドメインでWebSocketが拒否される
- **Let's EncryptのTLS証明書発行待ち**: 独自ドメインを新しく設定した直後は証明書発行に数分かかることがあり、その間はTLSエラーでWebSocketどころかHTTPSアクセス自体が不安定になる。焦らず数分待ってから再確認するにゃ
- **プロキシのアイドルタイムアウトとLiveViewのハートビート**: LiveViewはデフォルトで約30秒ごとにハートビートを送りWebSocketを維持しますが、プロキシ側のタイムアウト設定がそれより短いと定期的に切断される。Dokployのデフォルト設定では通常問題になりませんが、長時間接続したままにする使い方をする場合は覚えておくとよいにゃ

---

## 章末チェックリスト（総仕上げ）

- [ ] `force_ssl: [rewrite_on: [:x_forwarded_proto]]`を設定し、リダイレクトループが起きないことを確認した
- [ ] 本番URLでNetworkタブを開き、WSリクエストが`101 Switching Protocols`になっていることを目視確認した(`longpoll`にフォールバックしていない)
- [ ] セッションCookieに`Secure`/`same_site`が正しく設定され、WebSocket接続が安定していることを確認した
- [ ] Dokployからコンテナを手動再起動し、ブラウザが自動再接続すること、`StationCache`が再構築されることを確認した
- [ ] 第6章の`check_origin`、第2章の外部API疎通、第7〜8章の地図・Geolocation機能を、**この本番環境で一通り再確認**した
- [ ] 「ロジック層(第6章)」「JS連携層(第7,8章)」「インフラ層(第9章)」の3つが、全て同じ本番環境の上で揃って動くことを確認した

---

前章: [第8章 — Geolocation往復](./chapter_08.md)  
次章: [第10章 — 鯨アニメーション移植(任意)](./chapter_10.md)
